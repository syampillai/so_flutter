name: so_flutter
description: A wrapper around Flutter's App concepts. This package provides a small set of helper classes for quickly building Flutter applications based on Material design.
version: 0.3.14
homepage: https://github.com/syampillai/so_flutter

environment:
  sdk: '>=3.0.0 <=3.8.1'
  flutter: '>=3.29.3'

dependencies:
  flutter:
    sdk: flutter

  so_money: ^0.1.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
