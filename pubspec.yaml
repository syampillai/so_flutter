name: so_flutter
description: A wrapper around Flutter's App concepts.
version: 0.0.1
homepage: https://github.com/syampillai/so_flutter

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
